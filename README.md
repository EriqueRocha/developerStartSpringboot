cargo build --release

cargo wix init // basta rodar uma vez para gerar a pasta

choco install wixtoolset --version=3.14.0

cargo wix --bin-path "C:\Program Files (x86)\WiX Toolset v3.14\bin"

where candle
where light